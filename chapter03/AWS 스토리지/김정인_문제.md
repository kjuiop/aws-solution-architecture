Q-2) 비디오 처리를 전문으로 하는 중소기업에서 비용 절감을 위해 클라우드 스토리지를 검토하고 있다. 경영진은 고객의 파일을 온프레미스에서 클라우드로 완전히 옮기는 것을 우려하고 있지만, 현재는 비용에 중점을 두고 클라우드를 시험해보려고 한다. 중소기업이 AWS 를 시험해보기 가장 좋은 방법은?

A) 저장 볼륨 Storage Gateway를 설치한다.

- 스토리지를 선택할 때 로컬에 데이터를 저장할 수 있어야 한다는 것이 주요 고려사항
- ***저장 볼륨 Storage Gateway***
    - 파일을 로컬과 S3에 저장하면서 로컬의 사본을 캐시해서 사용할 수 있다.
    - 경영진은 원본 파일을 클라우드에 두는 것을 우려하고 있으므로 로컬파일을 데이터의 원본으로 사용하는 한편, 클라우드를 기본 스토리지로 사용하지 않으면서 클라우드 스토리지를 시험해볼 수 있다
- 캐시 볼륨 Storage Gateway
    - 파일을 로컬에 저장하고 백업으로 S3에 푸시한다.

Q-3) 웹 디자인 팀에서는 5GB 가 넘는 큰 압축 이미지 파일을 S3에 자주 업로드합니다. 최근 팀원이 업로드가 허용되는 최대 객체 크기를 초과합니다 라는 오류 메시지를 받았습니다. 이 문제를 해결하기 위해서는 어떤 조치를 해야합니까?

A) 웹 디자이너가 객체를 업로드 할 때 애플리케이션 또는 클라이언트에서 멀티 파트 업로드 API 를 활용하게 한다.

- 100MB 보다 큰 객체는 멀티파트 업로드 API 를 사용하는 것이 좋다.

Q-4) 다음 중 S3에서 최종 일관성이 있는 HTTP 메소드는 무엇입니까?

A) DELETE , 기존 객체 덮어쓰기 PUT

- 새로운 객체 PUT 은 쓰기 후 읽기 일관성이 있다.
- S3에서 DELETE 와 기존 객체 덮어쓰기 PUT 은 최종 일관성이 있다.

Q-5) S3 Standard 클래스에 저장할 수 있는 가장 작은 파일 크기는 ?

A) 0바이트

- S3 객체는 0바이트일 수 있다.

Q-7) us-east-2 리전에 ytmProfilePictures 라는 이름의 S3 버킷을 새로 맏늘고, 버킷의 루트에 images/ 라는 이름의 폴더를 만들었다. 웹사이트 호스팅을 활성화 했으며, 콘텐츠 팀이 images/ 폴더에 이미지를 업로드하도록 요청했다. 이에 접근 가능한 URL 은?

A) https://ymlProfilePictures.s3-website-us-ease-2.amazonaws.com/images

- S3 버킷에 직접 액세스하지 않고, S3에서 호스팅하는 웹사이트에 액세스한다는 점이다.
- 웹사이트 호스팅을 활성화하려면 버킷 이름에 FQDN 을 지정해야 한다.
- 버킷에 직접 액세스하려면 FQDN 맨 앞에 버킷 이름을 넣어서 구분해야 한다.
- 버킷 이름 다음에 FQDN 의 하위 도메인으로 s3-website 가 리전 코드와 -로 연결돼서 표기된다.

Q-13) 고객이 S3에 객체를 저장하는 비용을 문의해서 S3 버킷의 비용을 견적 중입니다. 현재 버킷에 저장된 객체의 크기는 0바이트~1GN입니다. S3-IA를 사용할 때 다음 중 비용이 청구되는 가장 작은 파일 크기는?

A) 128KB

- S3 는 0바이트 객체를 저장할 수 있고, 요금도 0바이트를 기준으로 청구한다.
- 하지만 S3-IA 에서는 128KB 보다 작은 용량의 객체도 128KB 로 요금을 청구한다.

Q-29) 다음 중 온프레미스 사이트와 클라우드 기반의 스토리지를 연결하는 서비스는?

A) Storage Gateway

- Storage Gateway 는 데이터를 캐싱 또는 저장하고 S3에 연결하는데 사용된다.
- 고객 게이트웨이는 Amazon VPN 연결의 고객 측 연결점이며, 가상 프라이빗 게이트웨이는 AWS 측 연결점이다.
- 가상 프라이빗 네트워크는 VPN 이다.

Q-30) 다음 중 Storage Gateway 의 기능은 무엇입니까?

A) 파일 게이트웨이, 볼륨 게이트웨이

- 클라우드 기반 스토리지에 연결하기 위한 솔루션을 제공한다.
- AWS 에 캐싱 게이트웨이라는 것은 없다.



**Q-31)** 솔루션 아키텍트는 ***NFS 기반 백업 시스템을 사용하고 있는 대기업에 스토리지 솔루션***을 추천하는 임무를 받았습니다. 이 회사는 클라우드 기반 스토리지를 검토하길 원하지만, ***기존에 투자한 백업 소프트웨어***도 포기하고 싶지 않습니다. 솔루션스 아키텍트는 어떤 유형의 Storage Gateway를 추천해야 합니까?

A) 파일 게이트웨이

- 파일 게이트웨이는 게이트웨이와 데이터를 송수신할 때 NFS 유형의 프로토콜을 사용한다.

**Q-33)** 중소기업에서 클라우드 기반 스토리지 솔루션을 테스트하는 임무를 맡았습니다. 경영진은 회사의 시스템에서 짧은 지연 시간으로 전체 데이터 세트에 즉각 액세스할 수 있기를 바랍니다. 어떤 Storage Gateway 유형이 이 요구사항을 만족합니까?

A) 저장 볼륨 게이트웨이

- 저장볼륨 게이트웨이는 데이터를 온프레미스 데이터 저장소에 저장하면서 비동기식으로 S3에 백업해서 재해 복구를 지원한다.
- 데이터를 로컬에 저장함으로써 네트워크 지연 시간을 최소화한다는 것이다. 저장 볼륨 게이트웨이만 데이터 세트 전체에 빠르게 액세스할 수 있도록 로컬 데이터를 제공한다.

**Q-34)** 최근 방대한 지리 공간 데이터 세트를 인수한 지도 제작 사업부에서 솔루션 아키텍처로 일하고 있습니다. 로컬 디스크 드라이브에 저장된 데이터 전체를 AWS 로 옮기려고 합니다. 10TB 가 넘는 데이터 세트를 AWS 로 옮기는 최선의 접근 방법은 무엇입니까?

A) Snowball

- 대규모 데이터를 AWS 로 이동해야 할 때는 항상 Snowball을 검토한다.
- Snowball은 네트워크를 통해 데이터를 전송하지 않으면서 AWS로 물리적 장치를 통해 데이터를 전달할 수 있다.
- 네트워크 중단이나 속도 저하를 유발하지 않는 유일한 솔루션

**Q-35)** 다음 중 캐싱 볼륨 Storage Gateway를 사용하는 이유로 옳은 것은?

A) 온사이트 스토리지의 비용을 줄이고자 한다.

A) 자주 사용하는 데이터에 짧은 지연 시간으로 액세스하고자 한다.

**Q-36)** 다음 중 기존의 백업 애플리케이션에 가장 적합한 Storage Gateway 는 무엇입니까?

A) 파일 게이트웨이

- 대개의 백업 솔루션은 NFS 나 파일 기반 백업을 사용하는데 이 경우에는 파일 게이트웨이가 가장 적합하다.

**Q-37)** 애플리케이션의 전체 데이터 세트 내에서 특정 부분에 지연시간이 발생합니다. 다음 중 이를 해결하는데 가장 적합한 Storage Gateway 는 무엇입니까?

A) 캐싱볼륨 게이트웨이

- 캐싱볼륨 게이트웨이는 전체 데이트 세트 내에서 특정 부분에서 지연시간이 발생한다.
- 가장 많이 사용하는 데이터를 온프레미스의 로컬 캐시에 저장한다.
- 전체 데이터 세트가 온프레미스에 있어야 하면 저장 볼륨 게이트웨이를 사용한다.

**Q-38)** 다음 중 전체 데이터 세트에 대한 지연 시간을 우선하는 애플리케이션에 가장 적합한 Storage Gateway 는 무엇입니까?

A) 저장볼륨 게이트웨이

- 전체 데이터 세트가 필요한 경우 캐싱 볼륨 게이트웨이보다 저장 볼륨 게이트웨이가 더 적합하다.
- 저장볼륨 게이트웨이는 전체 데이터 세트를 온프레미스에 저장하므로 모든 데이터에 빠르게 액세스 할 수 있다.

**Q-39)** 다음 중 원격지 재해 복구를 구축하는 비용을 줄이기 위해 가장 적합한 Storage Gateway는 무엇입니까?

A) 테이프게이트웨이

- 테이프 게이트웨이는 원격지 테이프 디렉터리를 대체하는데 이상적이다.
- 테이프 게이트웨이는 가상 테이프 디렉터리이며 실제 테이프를 원격 위치로 옮기는 비용을 줄일 수 있다.

Q-42) S3 버킷에 객체를 성공적으로 업로드하면 어떤 HTTP 코드가 반환됩니까?

A) HTTP 200

Q-43) S3 One Zone-IA의 내구성은 다음 중 무엇입니까?

A) 99.999999999%

- 모든 S3 스토리지 클래스 (S3 Standard, S3-IA, S3 One Zone-IA) 의 내구성은 모두 99.999999999% 이다.

Q-46) S3 One Zone-IA의 가용성은 무엇입니까?

A) 99.5%

- 모든 S3 스토리지 클래스의 내구성은 같지만, 가용성은 다르다.
- S3-IA 는 99.9%, S3 One Zone-IA는 99.5%, S3 Standard는 99.99%이다.

Q-49) 전송 중 데이터에 SSL 을 지원하는 S3 스토리지 클래스는 무엇입니까?

A) S3, S3-IA, S3 One Zone-IA

- S3 의 모든 스토리지 클래스는 전송 중 데이터에 SSL 과 저장 중 데이터에 암호화를 지원한다.

Q-50) 저장 중 데이터에 암호화를 지원하는 S3 스토리지 클래스는 무엇입니까?

A) S3, S3-IA, S3 One Zone-IA

- S3 의 모든 스토리지 클래스는 전송 중 데이터에 SSL 과 저장 중 데이터에 암호화를 지원한다.

Q-51) 다음 중 사용자가 리전을 지정해야 하는 스토리지 클래스는 무엇입니까?

A) 모두 해당 (S3, S3-IA, S3 One Zone-IA)

Q-54) 어떻게 S3 버킷에 저장된 데이터에 액세스 합니까?

A) 버킷에 REST 기반의 웹 서비스 인터페이스로 액세스

A) AWS Management Console 액세스

- S3는 SSH, SFTP, 표준 FTP 액세스를 제공하지 않는다. AWS Management Console이나 HTTP를 통한 REST 인터페이스를 통해 데이터에 액세스할 수 있다.

Q-55) 다음 중 S3에서 트래픽이 급증하면 데이터 액세스를 보장하기 위해 AWS 에서 작동하는 방식을 설명한 것은 무엇입니까?

A) S3는 서비스가 중단되지 않도록 자동으로 확장한다.

A) 요청 급증의 영향을 최소화하기 위해 AWS 네트워크에 균등하게 확장한다.

- S3는 애플리케이션이 많을 때 자동으로 확장되도록 설계된다.
- S3는 AWS 네트워크에서 균등하게 확장되는 경향이 있다.

Q-56) 솔루션스 아키텍트는 회사 외부 원격지에 있는 고비용의 스토리지를 AWS로 마이그레이션하는 임무를 맡았습니다. 온프레미스 환경에서는 파일을 로컬 NAS로 백업합니다. 다음 해당 파일을 원격지에 저장합니다. 저장소를 원격지에서 AWS로 옮길 예정이다. 회사는 내구성과 비용을 검토하고 있으며 파일에 빠른 액세스가 필요합니다. 추천할 수 있는 방법은?

A) NAS에서 S3 One Zone-IA 클래스 버킷으로 파일을 복사한다.

- 모든 S3 스토리지 내구성은 같다. 그러나 비용 면에서는 S3 Onw Zone-IA가 확실히 저렴하다. Glacier 가 더 저렴하지만, S3 One Zone-IA가 더 빠른 액세스를 제공한다.

Q-57) 향후 12개월 동안 기하급수적으로 성장할 것으로 예상하는 애플리케이션을 위해 스토리지를 구축하려고 합니다. 어떤 S3 스토리지 클래스를 추천해야 합니까?

A) 판단할 수 있는 충분한 정보가 없다.

- S3는 무한대의 확장성을 제공하며, 여러 스토리지 클래스의 확장성은 크게 차이가 없다.
- 데이터 검색 속도와 데이터 중요도를 알지 못하면 S3 Standard, S3-IA, Glacier 중에서 선택할 수 없다.

Q-59) S3에 객체를 업로드하는 기본 방식은 무엇입니까?

A) 한 번의 작업으로 업로드한다.

- 기본적으로 S3에서 하나의 PUT은 하나의 업로드 작업으로 이뤄진다.
- AWS는 100MB 가 넘으면 멀티 파트 업로드를 사용하도록 권장한다.

Q-60) 멀티 파일 업로드 API 를 통해 객체를 업로드해야 하는 경우는 다음 중 무엇입니까?

A) 10GB 이상의 파일을 업로드 할 때

- AWS 에서는 100MB 보다 큰 파일을 업로드할 때 멀티파일 업로드를 사용하도록 권장하고 있다.

Q-61) 멀티 파트 업로드를 사용할 때 고려해야 할 사항은 무엇입니까?

A) 안정적인 고대역 네트워크를 통해 대용량 객체를 업로드해서 대여폭 활용을 극대화한다.

A) 불규칙한 네트워크에서 모든 크기의 파일을 업로드해 복원력을 강화한다.

- 대용량 객체를 전송하기 위해서는 네트워크가 안정적이어야 한다.

Q-62) 다음 중 미리 서명된 URL과 일반 URL의 차이점에 대한 설명으로 옳은 것은 무엇입니까?

A) 미리 서명된 URL 에서는 URL 작성자가 제공한 권한을 특정 객체와 연결한다.

A) 미리 서명된 URL을 이용하면 AWS 자격 증명 없이 프라이빗 S3 버킷에 액세스할 수 있다.

- 미리 서명된 URL은 AWS 자격 증명이 없는 사용자가 특저어 리소스에 액세스할 수 있게 한다.
- 권한은 URL 자것ㅇ자가 부여한다.
- 자격 증명은 URL과 연결되어 있지만, URL 자체에 암호화되어 있는 것은 아니다.

Q-64) 미리 서명된 URL의 유효 기간은 다음 중 무엇입니까?

A) 설정하는 기간 동안

- 미리 서명된 URL 은 생성할 때 항상 유효한 TTL(Time to Live) 을 구성한다. 이 기간은 매우 짧거나 상당히 길 수 있다.

Q-66) 다음 중 S3가 버킷의 객체를 처리하는 방식과 일치하는 것은 무엇입니까?

A) Amazon S3에 새로운 객체를 저장하고, 즉시 해당 버킷의 키를 표시한다. 새로운 객체는 키 목록에 표시되지 않는다.

A) 객체를 삭제하고 즉시 읽기를 시도한다. S3는 여전히 삭제된 데이터를 반환한다.

A) 객체를 삭제하고 즉시 버킷의 키를 표시한다. S3는 삭제된 객체와 목록을 모두 반환한다.

Q-71) S3는 초당 몇 번의 PUT을 지원합니까?

A) S3 는 초당 3500 번 PUT 을 지원한다.

Q-73) 미국 동부(버지니아 북부)에서 만들어진 S3 버킷의 고유한 도메인 이름은 무엇입니까?

A) s3.amazonaws.com

- S3 는 S3 식별자(s3), 리전(여기서는 us-east-1), [amazonaws.com](http://amazonaws.com) 순으로 표시하고 그 뒤에 S3 버킷 이름을 표시한다.
- 즉, [https://s3-us-east-1.amazonaws.com/prototypeBucket32](https://s3-us-east-1.amazonaws.com/prototypeBucket32) 가 된다.
- 그러나, 미국 동부의 버킷은 특별하게 고유 엔드포인트를 [s3.amazonaws.com](http://s3.amazonaws.com) 으로 사용해야 한다.

Q-74) 다음 중 S3 버킷에 대한 도메인 이름으로 옳은 것은 무엇입니까?

A) s3-us-west-2.amazonaws.com

A) s3.amazonaws.com

- s3 와 리전은 ‘-’ 기호로 분리해야 한다.
- [s3.amazonaws.com](http://s3.amazonaws.com) 은 미국 동부(버지니아 북부)의 고유 URL이다.
- jp-west-2 라는 리전은 AWS 에 없다.

Q-75) AWS가 S3 버킷 액세스에서 지원하는 두 가지 방식의 URL은 무엇입니까?

A) 가상 호스팅 방식 URL

A) 경로 방식 URL

- S3는 가상 호스팅 방식 및 경로 방식의 두 가지 버킷 URL 방식을 지원한다.
- 가상 호스팅 방식 URL 은 [http://bucket.s3-aws-region.amazonaws.com](http://bucket.s3-aws-region.amazonaws.com) 형식
- 경로 방식 URL 은 [https://s3-aws-region.amazonaws.com/bucket-name](https://s3-aws-region.amazonaws.com/bucket-name) 방식이다.



Q-81) S3의 모든 객체에는 ( ) 이/가 있습니다. ( ) 에 들어갈 것은 무엇입니까?

A) 키

A) 값

A) 버전 ID

Q-82) S3 버킷에 있는 객체가 실수로 삭제되지 않게 하는 가장 좋은 방법은 무엇입니까?

A) 버킷에 대한 MFA Delete 를 활성화

- MFA Delete 는 객체가 실수로 삭제되지 않게 하는 가장 좋은 방법
- MFA 를 사용하면 객체를 삭제할 때 여러 형태의 인증을 요구한다.

Q-83) 다음 중 MFA Delete 요청으로 사용되는 HTTP 요청 헤더는 무엇입니까?

A) x-amz-mfa

- Amazon 특화 요청 헤더는 모두 x-amz 로 시작한다.
- x-amz-mfa

Q-84) MFA Delete를 활성화하면 다음 중 어떤 작업에 적용할 수 있습니까?

A) 버킷의 버전 관리 상태 변경

A) 영구적으로 객체 버전 삭제

- MFA Delete 는 버킷이 아닌 객체 삭제에 적용된다.
- 버킷의 버전관리 상태나 객체를 영구적으로 삭제하는데 영향을 준다.
- 객체를 그대로 유지하면서 객체의 메타데이터를 삭제하는 데는 MFA Delete 가 필요하지 않다.

Q-85) MFA Delete 를 활성화한 버킷을 사용하는 경우, 인증 코드는 어디서 받을 수 있습니까?

A) 하드웨어 또는 가상 MFA 장치

- MFA Delete 인증 코드는 Google Authenticator 같은 가상 MFA 장치 또는 하드웨어에서 가져온다.

Q-86) 누가 S3 버킷에서 MFA Delete 를 활성화할 수 있습니까?

A) 버킷을 소유한 루트 계정

- 버킷을 생성한 AWS Management Console 사용자라도 루트 사용자가 아니면 버킷에서 MFA Delete 를 활성화할 수 없다.

Q-87) 누가 S3 버킷에 버전 관리를 활성화 할 수 있습니까?

A) 버킷 소유자, 버킷을 소유한 루트계정, 버킷에 권한을 가진 모든 IAM 사용자

- 버킷 소유자, 루트 계정, 버킷에 권한을 가진 모든 IAM 사용자는 버전관리를 활성화할 수 있다.

Q-88) 다음 중 S3에 저장된 객체에 연결된 것은 무엇입니까?

A) 메타 데이터

A) 데이터

- S3의 각 객체에는 이름, 값(데이터), 버전 ID, 메타데이터가 있다.
- 버전 기록은 버전 관리를 설정하지 않으면 존재할 수 없다.

Q-89) AWS Management Console 을 사용해 객체에 메타 데이터를 추가하는 AWS 메커니즘은 무엇입니까?

A) 태그

- AWS 의 모든 메타 데이터는 이름-값 페어인 태그로 AWS Management Console에 입력한다.

Q-90) 다음 중 S3에서 객체의 모든 버전을 저장하지 않는 예외 경우는 무엇입니까?

A) 예외는 없다.

- 객체 삭제 방법과 관계없이 모든 버전의 객체가 저장된다.


Q-91) S3 버전 관리를 활성화한 뒤 버킷에서 버전 관리를 해제할 방법은 무엇입니까?

A) 버전 관리가 활성화된 후에는 버전 관리를 해제할 수 없다.

- S3 버킷에는 버전 관리가 활성화된 후에는 비활성화하거나 해제할 수 없다.
- 버전 관리는 일시 중단할 수는 있지만 실제로 버전 관리 기능은 해제되지 않으며 이전 버전은 유지된다.

Q-92) CloudFront 는 어떤 콘텐츠 유형을 배포하는 웹 서비스입니까?

A) 정적 파일

A) 스크립트나 프로그래밍으로 생성된 동적 콘텐츠

- CloudFront 는 원본 서버에서 정적 파일을 캐시해서 사용자 또는 클라이언트로 전달한다.
- EC2 또는 다른 웹서버로부터 CloudFront 를 통해 동적 컨텐츠를 제공할 수 있다.
- CloudFront 는 스토리지 시설이 아니라 배포 메커니즘이기 때문에 객체 기반 스토리지라는 것은 옳지 않다.

Q-94) CloudFront 배포에서 일반적으로 원본 서버가 될 수 있는 것은?

A) EC2 인스턴스

A) S3 버킷

- CloudFront 는 원본 서버의 컨텐츠 (일반적으로 정적 파일 및 동적 응답) 를 제공한다.
- 이러한 원본 서버는 주로 정적 콘텐츠는 S3 버킷이고, 동적 컨텐츠는 EC2 인스턴스이다.

Q-95) CloudFront 배포에서 일반적으로 원본 서버가 될 수 있는 것은 무엇입니까?

A) EC2에서 실행되는 Docker 컨테이너

A) S3 버킷

Q-97) CloudFront 배포에서 일반적으로 원본 서버가 될 수 있는 것은?

A) Elastic Load Balancer

A) Route 53 레코드 세트

- CloudFront 는 EC2와 직접 연결하지 않고 ELB와 연결해서 콘텐츠를 배포할 수 있다.
- Route53 레코드 세트와도 연결해서 콘텐츠를 배포할 수 있다.
- 이를 통해 콘텐츠가 여러 인스턴스에서 제공될 수 있다.

Q-98) 엣지 로케이션의 집합을 무엇이라고 합니까?

A) 배포

- CloudFront 배포는 전 세계 엣지 로케이션의 집합이다.



Q-101) CloudFront 를 사용하는 웹 애플리케이션에서 사용자에게 제공되는 콘텐츠를 저장하는 것은 무엇입니까?

A) 엣지 로케이션

A) EC2 인스턴스

- 가용영역은 콘텐츠 저장장치가 아니라 가상 데이터 센터이다.
- 엣지 로케이션은 CloudFront 배포에서 캐시된 콘텐츠를 저장하는데 사용된다.
- EC2 인스턴스는 프로세스로 콘텐츠를 제공할 수 있다.

Q-102) 다음 중 엣지 로케이션에 대한 설명으로 옳은 것은 무엇입니까?

A) 엣지 로케이션에서 읽기도 가능하다.

A) 엣지 로케이션에서 저장도 가능하다.

- 엣지 로케이션은 일반적으로 클라이언트에서 읽기로 사용하지만, 저장도 가능하다.
- 엣지 로케이션에 객체를 저장하고 읽을 수 있다.

Q-103) 다음 중 객체가 저장될 수 있는 곳은 어디입니까?

A) 엣지 로케이션

A) S3 버킷

- EC2 는 컴퓨팅 서비스이며, 스토리지가 아니라 객체가 저장될 수 없다.
- 엣지 로케이션에 객체를 저장할 수 있다.

Q-104) TTL은 무엇을 의미합니까?

A) Time To Live

- TTL 은 Time to Live 로, CloudFront 엣지 로케이션에서 객체가 캐싱되는 시간이다.

Q-107) S3 버킷을 새로 만들었을 때 기본 공개 수준은 무엇입니까?

A) 프라이빗

- 버킷을 만들면 기본적으로 비공개, 프라이빗이다.

Q-111) 다음 중 테이프 볼륨과 같은 것은 무엇입니까?

A) VTL

- VTL (가상 테이프 라이브러리) 이다. 테이프 볼륨은 가상 테이프 라이브러리이다.
- VPC 는 가상 사설 클라우드
- VPN 은 가상 사설 네트워크
- NetBackup 은 백업 애플리케이션이다.

Q-113) Snowball 이 지원하는 언어는 무엇입니까?

A) Snowball 은 실제 어떤 코드도 지ㅜ언하지 않는다.

- 데이터를 장치로 전송하고, Amazon 에 보내기만 하면 된다.

Q-114) Snowball 대신 AWS Dicrect Connect 를 사용하는 경우는 무엇입니까?

A) 일상에서 사용할 경우 AWS Direct Connect 가 Snowball 보다 좋은 선택이다.

- 온프레미스 네트워크와 AWS 간 전용 고속 연결이다. Direct Connect 는 데이터를 저장하고 되돌려보내는 Snowball 보다 더 나은 방법이다.

Q-116) 다음 중 Snowball 의 기능은 무엇입니까?

A) S3로 가져오거나 S3에서 내보내기

Q-118) 다음 중 데이터 웨어하우스의 분석 기능과 도구를 제공하는 AWS 서비스는 무엇입니까?

A) Redshift

- 데이터 웨어하우징에 적합한 OLAP 서비스이다.